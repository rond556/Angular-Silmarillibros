import { ArrayBreakerPipe } from './array-breaker.pipe';

describe('ArrayBreakerPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayBreakerPipe();
    expect(pipe).toBeTruthy();
  });
});
